{% if not rows or rows|length == 0 -%}
{{ empty_message or "Sem posições disponíveis." }}
{%- else -%}
{%- set r = rows[0] %}

**Resumo da posição em {{ r.ticker or identifiers.ticker or '-' }}**

- Data da posição: **{{ r.position_date|date_br if r.position_date is not none else '-' }}**
- Quantidade: **{{ r.qty|int_br if r.qty is not none else '-' }}**
- Valor da posição: **{{ r.position_value|currency_br if r.position_value is not none else '-' }}**
- Peso na carteira: **{{ r.weight_pct|percent_br if r.weight_pct is not none else '-' }}**
- DY 12m: **{{ r.dy_12m_pct|percent_br if r.dy_12m_pct is not none else '-' }}**
{%- endif %}
