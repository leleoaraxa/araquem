{% if not rows or rows|length == 0 -%}
{{ empty_message or "Nenhum registro encontrado." }}
{%- else -%}
{%- set r = rows[0] %}

**Histórico de yield do{{ ' ' + r.ticker if r.ticker else '' }}**

- Mês de referência: **{{ r.ref_month|date_br if r.ref_month is not none else '-' }}**
- Dividendos no mês: **{{ r.dividends_sum|currency_br if r.dividends_sum is not none else '-' }}**
- Preço de referência: **{{ r.price_ref|currency_br if r.price_ref is not none else '-' }}**
- DY mensal: **{{ r.dy_monthly|percent_br if r.dy_monthly is not none else '-' }}**

{%- endif %}
