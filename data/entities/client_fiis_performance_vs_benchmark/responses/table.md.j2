{% if not rows or rows|length == 0 -%}
{{ empty_message or "Sem histórico de performance para exibir." }}
{%- else -%}
{%- set r = rows[0] %}

**Performance da carteira vs {{ r.benchmark_code or '-' }}**

- Data de referência: **{{ r.date_reference|date_br if r.date_reference is not none else '-' }}**
- Retorno da carteira: **{{ r.portfolio_return_pct|percent_br if r.portfolio_return_pct is not none else '-' }}**
- Retorno do benchmark: **{{ r.benchmark_return_pct|percent_br if r.benchmark_return_pct is not none else '-' }}**
- Valor da carteira: **{{ r.portfolio_amount|currency_br if r.portfolio_amount is not none else '-' }}**
{%- endif %}
