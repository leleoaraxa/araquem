{% if rows and rows|length > 0 -%}
Lista ({{ rows|length }}):
{%- for r in rows %}
- {{ r[fields.key] }} — {{ r[fields.value] }}
{%- endfor %}
{%- else -%}
{{ empty_message or "Nenhum registro encontrado." }}
{%- endif %}
