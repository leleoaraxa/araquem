{% if not rows or rows|length == 0 -%}
{{ empty_message or "Sem posições disponíveis." }}
{%- else -%}
{%- set r = rows[0] %}

**Posição do cliente em {{ r.ticker or identifiers.ticker or '-' }}**

- Data da posição: **{{ r.position_date|date_br if r.position_date is not none else '-' }}**
- Quantidade: **{{ r.qty|int_br if r.qty is not none else '-' }}**
- Preço de fechamento: **{{ r.closing_price|currency_br if r.closing_price is not none else '-' }}**
- Peso na carteira: **{{ r.percentage|percent_raw_br if r.percentage is not none else '-' }}**
- Mês/Ano Referência: **{{ r.month_number or '-' }}/{{ r.year_number or '-' }}**
{%- endif %}
