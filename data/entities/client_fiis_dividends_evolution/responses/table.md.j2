{% if not rows or rows|length == 0 -%}
{{ empty_message or "Sem histórico de dividendos para exibir." }}
{%- else -%}
{%- set r = rows[0] %}

**Evolução de dividendos da carteira do cliente**

- Referência: **{{ r.month_name or r.month_number or '-' }}/{{ r.year_reference or '-' }}**
- Total no mês: **{{ r.total_dividends|currency_br if r.total_dividends is not none else '-' }}**
- Documento: **{{ r.document_number or '-' }}**
{%- endif %}
