{% if not rows or rows|length == 0 -%}
{{ empty_message or "Nenhum registro encontrado." }}
{%- else -%}
{%- set r = rows[0] %}

**Dividendos do{{ ' ' + r.ticker if r.ticker else '' }}**

- Data de pagamento: **{{ r.payment_date|datetime_br if r.payment_date is not none else '-' }}**
- Data-com (cutoff): **{{ r.traded_until_date|datetime_br if r.traded_until_date is not none else '-' }}**
- Valor distribuído por cota: **{{ r.dividend_amt|currency_br if r.dividend_amt is not none else '-' }}**
- Registro criado em: **{{ r.created_at|datetime_br if r.created_at is not none else '-' }}**, atualizado em **{{ r.updated_at|datetime_br if r.updated_at is not none else '-' }}**

{%- endif %}
