{% if rows and rows|length > 0 %}

{% set last = rows[0] %}
O **{{ ticker }}** está sendo negociado em torno de **{{ last.close_price | currency_br }}**, com variação de **{{ last.daily_variation_pct | percent_raw_br }}** no pregão mais recente.

Segue um resumo recente de preços:

| Data | Preço (R$) | Variação no dia |
|------|-------------|------------------|
{% for r in rows[:6] %}
| {{ r.traded_at | date_br }} | {{ r.close_price | currency_br }} | {{ r.daily_variation_pct | percent_raw_br }} |
{% endfor %}

{% else %}
Não encontrei dados de preço para **{{ ticker }}**.
{% endif %}
