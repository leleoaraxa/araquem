{% if rows and rows|length > 0 %}
As últimas notícias do **{{ ticker }}** são:

| Fonte | Título | Tags | Link |
|-------|--------|------|------|
{% for r in rows[:5] %}
{% if r.tags %}
  {% if r.tags is string %}
    {% set tags_value = r.tags %}
  {% else %}
    {% set tags_value = r.tags | join("; ") %}
  {% endif %}
{% else %}
  {% set tags_value = "-" %}
{% endif %}

| {{ r.source }} | {{ r.title }} | {{ tags_value }} | {{ r.url }} |
{% endfor %}
{% else %}
Não encontrei notícias recentes sobre **{{ ticker }}**.
{% endif %}
