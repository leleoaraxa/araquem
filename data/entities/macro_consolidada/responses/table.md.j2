{% if rows and rows|length > 0 -%}
| Data ref. | IPCA | SELIC | CDI | IFIX (pts) | IFIX var. (%) | IBOV (pts) | IBOV var. (%) | USD compra (R$) | USD venda (R$) | USD var. (%) | EUR compra (R$) | EUR venda (R$) | EUR var. (%) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
{%- for r in rows %}
{%- set ipca_val = r.ipca if r.ipca is not none else None %}
{%- set cdi_val = r.cdi if r.cdi is not none else None %}
| {{ r.ref_date|date_br }} | {{ ipca_val if ipca_val is not none else '-' }} | {{ r.selic }} | {{ cdi_val if cdi_val is not none else '-' }} | {{ r.ifix_points }} | {{ r.ifix_var_pct|percent_br }} | {{ r.ibov_points }} | {{ r.ibov_var_pct|percent_br }} | {{ r.usd_buy_amt|currency_br }} | {{ r.usd_sell_amt|currency_br }} | {{ r.usd_var_pct|percent_br }} | {{ r.eur_buy_amt|currency_br }} | {{ r.eur_sell_amt|currency_br }} | {{ r.eur_var_pct|percent_br }} |
{%- endfor %}
{%- else -%}
{{ empty_message or "Sem histórico macro para exibir." }}
{%- endif %}
