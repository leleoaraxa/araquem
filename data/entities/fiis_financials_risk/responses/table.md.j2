{% if not rows or rows|length == 0 -%}
{{ empty_message or "Nenhum registro encontrado." }}
{%- else -%}
{%- set r = rows[0] %}

**Indicadores de risco do{{ ' ' + r.ticker if r.ticker else '' }}**

- Volatilidade: **{{ r.volatility_ratio|number_br if r.volatility_ratio is not none else '-' }}**
- Sharpe / Sortino: **{{ r.sharpe_ratio|number_br if r.sharpe_ratio is not none else '-' }} / {{ r.sortino_ratio|number_br if r.sortino_ratio is not none else '-' }}**
- Treynor e Alfa de Jensen: **{{ r.treynor_ratio|number_br if r.treynor_ratio is not none else '-' }} / {{ r.jensen_alpha|number_br if r.jensen_alpha is not none else '-' }}**
- Beta: **{{ r.beta_index|number_br if r.beta_index is not none else '-' }}**
- Máx. drawdown (MDD): **{{ r.max_drawdown|percent_br if r.max_drawdown is not none else '-' }}**
- R²: **{{ r.r_squared|number_br if r.r_squared is not none else '-' }}**
- Atualizado em: **{{ r.updated_at|datetime_br if r.updated_at is not none else '-' }}**

{%- endif %}
