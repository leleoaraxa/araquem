{% if rows and rows|length > 0 -%}
{# Mostra até 10 itens mais recentes, com a mesma linha editorial do templates.md #}
{%- set limit = 10 -%}
{%- for r in rows[:limit] -%}
- {{ r.published_at|datetime_br }}: {{ r.title }} — {{ r.source }} ({{ r.url }})
{%- endfor -%}
{%- else -%}
{{ empty_message or "Nenhuma notícia encontrada." }}
{%- endif %}
