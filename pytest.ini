[pytest]
addopts = -q
testpaths = tests
python_files = test_*.py
pythonpath = .
log_cli = true
log_cli_level = INFO
filterwarnings =
    ignore::DeprecationWarning

asyncio_default_fixture_loop_scope = function

markers =
    integration: testes que dependem de serviços externos (ex.: Postgres)
